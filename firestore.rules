rules_version = '2';

// Every read and write goes through the Next.js server using admin
// credentials, which bypass these rules entirely. No browser should ever
// talk to Firestore directly, so nothing is allowed here.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
